check process pg-business with pidfile /data/pg-business/postmaster.pid
  start program = "/bin/su - luminance -c '/usr/local/bin/pg_ctl -D /data/pg-business -l /data/log/pg-business.log start'"
  stop  program = "/bin/su - luminance -c '/usr/local/bin/pg_ctl -D /data/pg-business stop'"
  if failed host localhost port 5432 type TCP then restart
  if 3 restarts within 5 cycles then timeout
