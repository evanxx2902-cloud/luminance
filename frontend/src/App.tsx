// Scaffold placeholder — replace with real routes when building business features.
export default function App() {
  return (
    <main className="min-h-screen bg-surface flex items-center justify-center">
      <div
        className="bg-white rounded-clay border-clay border-[3px] p-10 max-w-md w-full text-center"
        style={{
          borderColor: 'var(--color-primary)',
          boxShadow:   'var(--shadow-clay)',
        }}
      >
        <h1 className="font-heading text-4xl font-bold text-primary mb-3">
          Luminance
        </h1>
        <p className="font-body text-text text-lg">
          教育平台 — 脚手架已就绪
        </p>
        <p className="font-body text-secondary text-sm mt-2">
          Educational Platform scaffold ready.
        </p>
      </div>
    </main>
  )
}
